---
import type { Spacing } from "../style/theme";
import type { AsProp } from "../types/component/AsProp";
import { toStyle } from "../utils/style";

type Props = {
  gap?: Spacing;
  class?: string;
  direction?: "row" | "column";
  alignItems?: CSSStyleDeclaration["alignItems"];
  justifyContent?: CSSStyleDeclaration["justifyContent"];
  height?: CSSStyleDeclaration["height"];
  wrap?: boolean;
} & AsProp;

const {
  gap,
  class: className,
  direction,
  alignItems,
  justifyContent,
  height,
  wrap,
  as: Tag = "div",
} = Astro.props;

const horizontal = direction === "row";

const style = toStyle({ gap, alignItems, justifyContent, height });
---

<Tag class:list={["stack", className, { horizontal, wrap }]} style={style}>
  <slot />
</Tag>

<style>
  .stack {
    display: flex;
    flex-direction: column;
  }
  .stack.horizontal {
    flex-direction: row;
  }
  .stack.wrap {
    flex-wrap: wrap;
  }
</style>
