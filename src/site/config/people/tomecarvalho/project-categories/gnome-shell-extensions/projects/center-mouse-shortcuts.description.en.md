A simple extension that adds two configurable keyboard shortcuts to centre the mouse pointer on the focused window or display, similar to KDE Plasma's built-in shortcuts.

Useful when the mouse pointer finds itself inconveniently far from the window you have focused.
